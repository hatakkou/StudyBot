# 勉強サーバー用 Discord Bot 計画（草案）

## 概要

高校の友人と作る勉強用 Discord サーバー向け bot。
「できるだけ楽に使えること」を最優先にする。
→ 操作は**ボタン中心**、**ミュート操作をトリガーにした自動記録**、スラッシュコマンドは保険として併用。

---

## 1. 自動勉強時間記録（メイン機能）

### 1-1. トリガー設計（楽さ優先・複数方式を併用）

| 方式 | どう動くか | 楽さ | 精度 | 備考 |
|---|---|---|---|---|
| **ボタン（常設メッセージ）** | チャンネルに固定メッセージを置き「勉強開始 / 休憩 / 終了」ボタンを用意 | ◎ クリック1回 | ◎ | **メイン UI** として採用。科目もボタン or セレクトで選択 |
| **ミュート/アンミュート連動** | VC で**ミュート → 勉強中**、**ミュート解除 → 休憩/雑談中**と自動判定 | ◎ 操作ゼロ | △ 暗黙ルールが必要 | `voiceStateUpdate` イベントで検知。サーバー内で「ミュート = 集中」の約束事を共有する |
| **VC 入退室** | 記録対象 VC に入ったら開始、出たら終了 | ◎ | △ 雑談だけで入室しても記録される | 記録対象 VC を「勉強部屋」に限定すれば精度 UP |
| **スラッシュコマンド** | `/start` `/end` `/pause` `/resume` | ◯ | ◎ | ボタンが押せない場面（DM やスマホ等）の保険 |


**設計のポイント**
- 「完全集中」モード：自己ミュート + ヘッドホンミュート（自分に聞こえないようにする操作）の両方を検知すれば「完全に聞こえない状態 = ガチ集中」と判定できる。
- ミュートは「サーバー内の合図」として使う。雑談したい人はミュートを外す、という運用。
- **イベント発生時に都度 DB へ書き込む**設計にする（bot が落ちても記録が消えない）。
- bot 再起動時は `guild.voiceStates` をスキャンして「VC に居るのに記録が無い人」の未終了セッションを復元する。

### 1-2. 記録と集計

- **セッション管理**：開始 / 終了 / 休憩 / 再開。休憩時間は勉強時間から除外
- **科目タグ**：開始時に「数学」「英語」「理科」などから選択（ただしめんどくさいので任意） → 科目別に集計できる
- **ポモドーロ**：25 分集中 + 5 分休憩のタイマー。サーバー全体で同期し、休憩になったら VC 移動やアンミュート通知
- **集計**：今日 / 今週 / 全期間、科目別
- **リーダーボード**：週間ランキング（健全な競争はモチベになる。やりすぎ注意）
- **レポート**：夜に「今日の勉強時間」をチャンネルへ投稿。週報も出す（コマンド経由）


---

## 2. リマインダー機能

- **自然言語風の指定**：`/remind あと30分` `/remind 22:00` `/remind 毎週月曜19時`
- **定期リマインド**：毎日 23 時に「そろそろ寝よう」、勉強開始時間の通知など
- **ボタン操作**：通知に「スヌーズ（10 分後にもう一度）/ 完了」ボタン


## 3. 追加アイデア（あったらいいもの）

- **BGM / ホワイトノイズ**：ボイスチャンネルにローファイ等を流す（既存の Lofi Radio 等で代替も可）

